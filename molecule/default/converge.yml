---
- name: Converge
  hosts: all
  become: true

  tasks:
    - file:
        path: /tmp/test
        state: directory

    - name: "Include grycap.nfs"
      include_role:
        name: "ansible-role-nfs"
      vars:
        nfs_mode: 'front'
        nfs_exports: [{path: "/home", export: "wn*(fsid=0,rw,async,no_root_squash,no_subtree_check,insecure)"}]

    - name: "Include grycap.nfs"
      include_role:
        name: "ansible-role-nfs"
      vars:
        nfs_mode: 'wn'
